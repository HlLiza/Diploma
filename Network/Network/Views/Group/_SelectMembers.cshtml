
@model IEnumerable<Network.Views.ViewModels.UserListViewModel>


@*@section Scripts{
    <script type="text/javascript">
        $(document).ready(function () {

        });

        function addMember(element) {
            debugger
            var url = "Group/AddMembers";
            var grId = $("#groupId").val();

            $.post(url, { userId: element, groupId: grId }).done()
            {
                $("#userList").remove(usId);
            };

        }

    </script>
}*@




    @if (Model.Count() > 0)
    {
        using (Html.BeginForm("AddMembers", "Group", FormMethod.Post, null))
        {
            <div id="id="userList"">
                        @foreach (var item in Model)
                        {
                                <div  class="row item-user">
                                    <div class="col-md-1 form-check medium" style="padding: 70px 30px 30px 60px;">
                                        <button id="@item.Id" onclick="window.addMember(this)" class="form-check-input "> <</button>>                                    </div>
                                    <div class="col-md-3">
                                        @if (item.Image != null)
                                        {
                                            <div>
                                                <img class="user-img" src="data:image/png;base64, @Convert.ToBase64String(item.Image)" />
                                            </div>
                                        }
                                        else
                                        {
                                            <div>
                                                <img class="user-img" src="@Url.Content("~/Content/Pictures/default.jpeg")" alt="">
                                            </div>
                                        }
                                    </div>
                                    <div class="col-md-4" style="padding-top: 72px;">
                                        @Html.DisplayFor(modelItem => item.Surname)
                                        @Html.DisplayFor(modelItem => item.Name)
                                    </div>
                                    <div class="col-md-3" style="padding-top: 72px;">
                                        @Html.DisplayFor(modelItem => item.Direction)
                                    </div>
                                </div>
                                <hr />
                        }
                </div>

        }
    }
    else
    {
        <div class="row item-user" style="padding-left: 35px;">
            НЕТ ПОЛЬЗОВАТЕЛЕЙ ПО ЗАДАННОМУ НАПРАВЛЕНИЮ!
        </div>
    }




